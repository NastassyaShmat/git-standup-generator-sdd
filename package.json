{
  "name": "sdd-study",
  "version": "0.1.0",
  "description": "Spec-Driven Development methodology comparison — building git-standup-generator four ways",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "git-standup": "methodologies/openspec/bin/git-standup"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'methodologies/openspec/tests/**/*.test.ts'"
  },
  "keywords": [
    "sdd",
    "spec-driven-development",
    "spec-kit",
    "openspec",
    "kiro",
    "bmad"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
